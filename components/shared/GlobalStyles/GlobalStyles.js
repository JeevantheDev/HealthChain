import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  bottomTabContainer: {
    position: 'absolute',
    bottom: 20,
    left: 20,
    right: 20,
    elevation: 0,
    borderRadius: 10,
    height: 60,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { GlobalStyles };
